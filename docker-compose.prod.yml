services:

  caddy:
    build:
      context: ./caddy
      dockerfile: Dockerfile
    container_name: merki-caddy
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    environment:
      - DOMAIN=${DOMAIN}
      - CLOUDFLARE_API_TOKEN=${CLOUDFLARE_API_TOKEN}
    depends_on:
      server:
        condition: service_started
      auth-server:
        condition: service_started
      web:
        condition: service_started
    networks:
      - merki-network
    restart: unless-stopped

  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: merki-server
    environment:
      PORT: ${PORT}
      SERVER_HOST: ${SERVER_HOST}
      SERVER_READ_TIMEOUT: ${SERVER_READ_TIMEOUT}
      SERVER_WRITE_TIMEOUT: ${SERVER_WRITE_TIMEOUT}
      DATABASE_URL: postgres://${DATABASE_USER}:${DATABASE_PASSWORD}@postgres:5432/${DATABASE_NAME}?sslmode=disable
      REDIS_URL: redis://redis:6379/${REDIS_DB}
      REDIS_DB: ${REDIS_DB}
      REDIS_PASSWORD: ${REDIS_PASSWORD}
      BETTER_AUTH_URL: http://auth-server:3001
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
      AWS_REGION: ${AWS_REGION}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY}
      AWS_S3_BUCKET_NAME: ${AWS_S3_BUCKET_NAME}
      APP_ENV: production
      APP_DEBUG: false
      APP_ALLOWED_ORIGINS: https://${DOMAIN},https://admin.${DOMAIN},https://api.${DOMAIN}
      RESEND_API_KEY: ${RESEND_API_KEY}
      RESEND_FROM_EMAIL: ${RESEND_FROM_EMAIL}
      RESEND_FROM_NAME: ${RESEND_FROM_NAME}
      EMAIL_IMAGE_BASE_URL: ${EMAIL_IMAGE_BASE_URL:-https://${DOMAIN}}
      EMAIL_APP_URL: ${EMAIL_APP_URL:-https://${DOMAIN}}
      EMAIL_SUPPORT_EMAIL: ${EMAIL_SUPPORT_EMAIL:-soporte@${DOMAIN}}
      EMAIL_SUPPORT_PHONE: ${EMAIL_SUPPORT_PHONE:-+58 412-XXX-XXXX}
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - merki-network
    restart: unless-stopped

  postgres:
    image: postgres:15-alpine
    container_name: merki-postgres
    environment:
      POSTGRES_USER: ${DATABASE_USER}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
      POSTGRES_DB: ${DATABASE_NAME}
    volumes:
      - merki_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DATABASE_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - merki-network
    restart: always

  redis:
    image: redis:7-alpine
    container_name: merki-redis
    volumes:
      - merki_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - merki-network
    restart: always

  auth-server:
    build:
      context: ./auth-server
      dockerfile: Dockerfile
    container_name: merki-auth-server
    environment:
      PORT: 3001
      DATABASE_URL: postgres://${DATABASE_USER}:${DATABASE_PASSWORD}@postgres:5432/${DATABASE_NAME}?sslmode=disable
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      CORS_ORIGINS: https://${DOMAIN},https://admin.${DOMAIN},https://api.${DOMAIN}
      TRUSTED_ORIGINS: https://${DOMAIN},https://admin.${DOMAIN}
      GO_BACKEND_URL: http://server:8080
      INTERNAL_API_KEY: ${INTERNAL_API_KEY}
      RESET_PASSWORD_URL: ${RESET_PASSWORD_URL}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - merki-network
    restart: unless-stopped

  web:
    build:
      context: ./web
      dockerfile: Dockerfile
      args:
        VITE_BETTER_AUTH_URL: https://auth.${DOMAIN}/api/auth
        VITE_GO_BACKEND_URL: https://api.${DOMAIN}/api/v1
    container_name: merki-web
    networks:
      - merki-network
    restart: unless-stopped

volumes:
  merki_db_data:
  merki_redis_data:
  caddy_data:
  caddy_config:

networks:
  merki-network:
    driver: bridge
